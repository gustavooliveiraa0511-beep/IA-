# === Bot Telegram ===
python-telegram-bot==21.6

# === IA: roteiro ===
groq==0.11.0

# === IA: narração ===
# Edge TTS é primário (vozes brasileiras excelentes).
# Se Microsoft bloquear (403 em IPs de cloud), cai pro gTTS (Google).
edge-tts>=7.0.0
gTTS==2.5.3

# === IA: transcrição (timestamp por palavra) ===
# Usamos SÓ faster-whisper (mais rápido, vem com wheels pré-compilados).
# openai-whisper foi removido porque tenta compilar do source e falha
# no GitHub Actions por falta de pkg_resources.
faster-whisper==1.0.3

# === Busca de mídia ===
requests==2.32.3
wikipedia-api==0.7.1

# === Edição de vídeo ===
ffmpeg-python==0.2.0
Pillow==10.4.0

# === Storage ===
boto3==1.35.36  # Cloudflare R2 é S3-compatible

# === Utilidades ===
python-dotenv==1.0.1
pydantic==2.9.2
httpx==0.27.2
tenacity==9.0.0  # retry logic
rich==13.9.2  # logs bonitos
